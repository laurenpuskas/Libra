import React from 'react'

import { StyledContent } from '../styles/MainContent'

const MainContent = (props) => {
  return <StyledContent>{props.children}</StyledContent>
}

export default MainContent
