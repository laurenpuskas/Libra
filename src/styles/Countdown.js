import styled from 'styled-components'

export const StyledTimer = styled.p`
    font-size: 2.8rem;
    font-weight: 900;

    span {
        font-size: 1.8rem;
        letter-spacing: -0.5px;
    }
`
